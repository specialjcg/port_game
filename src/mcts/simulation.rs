// MCTS Simulation helpers

// Placeholder for future simulation logic
// Currently simulation is handled in the main MCTS engine

pub struct Simulator;

impl Simulator {
    pub fn new() -> Self {
        Self
    }
}

impl Default for Simulator {
    fn default() -> Self {
        Self::new()
    }
}
